import 'package:flutter/material.dart';

// Colors

const hBlueColor = const Color.fromRGBO(158, 181, 199, 1.0);
const hMainGreen = const Color.fromRGBO(140, 216, 207, 1.0);
const hFadedGreen = const Color.fromRGBO(140, 216, 207, 0.5);
const hWhite = Colors.white;

// Cubic Curves
const cubicEase = const Cubic(0.8, 0, 0.2, 1);

// Duration
const hDuration300 = const Duration(milliseconds: 300);
const hDuration700 = const Duration(milliseconds: 700);
