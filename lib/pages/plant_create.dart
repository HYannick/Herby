import 'package:flutter/material.dart';

class PlantCreatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Plant Create page'),
    );
  }
}
